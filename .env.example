# Base URL del API desde el navegador.
# En `astro dev`: usa /api (el proxy en astro.config.mjs reenvía al Nest en LAN; evita CORS).
PUBLIC_API_BASE_URL=/api

# Sin proxy (p. ej. preview estático o prod sin Nginx): URL absoluta del backend.
# PUBLIC_API_BASE_URL=http://10.80.80.219:2099
