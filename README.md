# AI Support Integration Test

Proyecto de **chatbox de soporte** que integra una **IA local (Ollama)** en un servidor Ubuntu, un **backend API (NestJS + TypeScript)** y un **frontend (Astro)**. El backend actúa como intermediario entre el cliente y Ollama 